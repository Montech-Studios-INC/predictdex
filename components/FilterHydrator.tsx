"use client";

type Props = {
  country?: string | null;
  category?: string | null;
};

// Temporarily disabled while we refine the filter synchronisation.
export default function FilterHydrator(_props: Props) {
  return null;
}
